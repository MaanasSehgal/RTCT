const Board = ({boardData, selectedTab} : any) => {
    return (
        <div className="w-full h-full bg-[#131217] overflow-y-auto">
            {(boardData)}
        </div>
    )
}

export default Board;